# 2026-07-28
- 排查 TeProjectApplication "启动不了"：实测项目本身正常，`mvn.cmd spring-boot:run` 成功启动（0.8s）后正常退出。
- 项目仅依赖 spring-boot-starter（无 web），启动完成即退出属正常现象；若需常驻服务应加 spring-boot-starter-web。
- 环境情况：PATH 默认 java 是 1.8.0_481，JAVA_HOME 是 JDK 11（Microsoft jdk-11.0.29）；本地 Maven 3.9.9 在 F:/apache-maven-3.9.9。
- 坑：Git Bash 下直接调 `mvn`（shell 脚本）报 ClassNotFoundException: plexus Launcher，需用 `mvn.cmd`。
- 用户已在 pom 中自行加入 spring-boot-starter-web，项目现为 Web 应用。
- 验证打包：`mvn.cmd package` 成功产出可执行 fat jar（target/TeProject-0.0.1-SNAPSHOT.jar，17M），`java -jar --server.port=18080` 启动正常（Tomcat 9.0.83）。
- pom 无缺失配置：spring-boot-maven-plugin 的 repackage 已生效。
- 注意：本沙箱环境注入了 SERVER__PORT=58894 环境变量，会覆盖默认端口且该端口被占用，测试时需显式 --server.port。
- 新增 DemoController（src/main/java/com/fjf/teproject/controller/DemoController.java）：3 个 GET 接口 /api/demo/hello、/api/demo/greet?name=、/api/demo/user/{id}，统一返回 {code, message, data} 结构。已编译通过并 curl 实测全部返回正常。
- 新增 stop.bat / stop.sh 脚本：通过 jps 识别 TeProjectApplication PID 后关闭，用于快速停止本地 Spring Boot 服务。
